# Everything the app needs, from a clone: `pip install -r requirements.txt`.
# The first line installs the Maia-3 model code straight from GitHub so there is
# nothing else to clone. It lives here because it is not on PyPI.

maia3 @ git+https://github.com/CSSLab/maia3

# The package itself, editable. This is what puts the `chessformer_lens` command
# on your PATH; torch, numpy, python-chess, huggingface-hub, ipython and both
# frontends (pywebview + matplotlib) come along as its dependencies.
-e .
